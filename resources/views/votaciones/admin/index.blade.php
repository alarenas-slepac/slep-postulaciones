@extends('layouts.app')
@section('content')
<div class="container py-4"><div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 mb-1"><i class="bi bi-geo-alt-fill me-2"></i>Votaciones CCAF y Mutualidades</h1><p class="text-muted mb-0">Configuración y seguimiento de jornadas.</p></div><div class="d-flex gap-2"><a class="btn btn-outline-primary" href="{{ route('votaciones.operacion.index') }}">Operación móvil</a><a class="btn btn-primary" href="{{ route('votaciones.admin.jornadas.create') }}"><i class="bi bi-plus-circle me-1"></i>Nueva jornada</a></div></div>
@include('votaciones.partials.alertas')
<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover align-middle mb-0"><thead><tr><th>Jornada</th><th>Fecha</th><th>Procesos</th><th>Grupos</th><th>Estado</th><th></th></tr></thead><tbody>@forelse($jornadas as $jornada)<tr><td><strong>{{ $jornada->nombre }}</strong><br><small class="text-muted">{{ $jornada->slug }}</small></td><td>{{ $jornada->fecha->format('d-m-Y') }}</td><td>{{ $jornada->procesos()->pluck('codigo')->join(', ') }}</td><td>{{ $jornada->grupos_count }}</td><td><span class="badge text-bg-{{ $jornada->estado==='finalizada'?'success':($jornada->estado==='borrador'?'secondary':'primary') }}">{{ str($jornada->estado)->replace('_',' ')->title() }}</span></td><td class="text-end"><a class="btn btn-sm btn-outline-primary" href="{{ route('votaciones.admin.jornadas.show',$jornada) }}">Administrar</a></td></tr>@empty<tr><td colspan="6" class="text-center text-muted py-5">No existen jornadas.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $jornadas->links() }}</div></div>
@endsection
