@extends('layouts.app')
@section('content')
<div class="container py-4"><div class="mb-4"><h1 class="h3 mb-1"><i class="bi bi-sign-turn-right-fill me-2"></i>Operación de votaciones</h1><p class="text-muted">Grupos asignados en jornadas activas.</p></div>@include('votaciones.partials.alertas')<div class="row g-3">@forelse($grupos as $grupo)<div class="col-md-6 col-xl-4"><a href="{{ route('votaciones.operacion.show',$grupo) }}" class="card shadow-sm h-100 text-decoration-none text-body"><div class="card-body"><span class="badge text-bg-primary mb-2">{{ str($grupo->estado)->replace('_',' ')->title() }}</span><h2 class="h5">{{ $grupo->nombre }}</h2><p class="mb-1">{{ $grupo->jornada->nombre }}</p><small class="text-muted">{{ $grupo->rutas->where('visita.estado','finalizada')->count() }} de {{ $grupo->rutas->count() }} visitas finalizadas</small></div></a></div>@empty<div class="col-12"><div class="alert alert-info">No tienes grupos activos asignados.</div></div>@endforelse</div></div>
@endsection
